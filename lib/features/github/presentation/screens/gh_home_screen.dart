import 'package:flutter/material.dart';
class GithubHomeScreen extends StatelessWidget {
  const GithubHomeScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Not available')));
}
